<section class="section border-top">
    <div class="container grid-process">
        <div class="sticky-col">
            <h2>
                Cómo funciona el <span class="text-primary">LanzaSaaS Lab</span>
            </h2>
            <p class="desc">Proceso claro y profesional.</p>

            <div class="principles-card">
                <h4>
                    <span class="material-symbols-outlined text-primary"
                        >verified</span
                    > Principios
                </h4>
                <ul>
                    <li><span class="dot"></span> Nada se fuerza.</li>
                    <li><span class="dot"></span> Todo se mide.</li>
                    <li><span class="dot"></span> Todo deja aprendizaje.</li>
                </ul>
            </div>
        </div>

        <div class="timeline">
            <div class="timeline-item">
                <div class="marker"></div>
                <h4>1. Propuesta</h4>
                <p>Un Founder propone una idea.</p>
            </div>
            <div class="timeline-item">
                <div class="marker"></div>
                <h4>2. Validación del Problema</h4>
                <p>Se valida que el problema existe en el mercado.</p>
            </div>
            <div class="timeline-item">
                <div class="marker"></div>
                <h4>3. Formación del Equipo</h4>
                <p>Se forma el equipo (Tech + Marketing).</p>
            </div>
            <div class="timeline-item">
                <div class="marker"></div>
                <h4>4. Construcción MVP</h4>
                <p>Se construye un Producto Mínimo Viable.</p>
            </div>

            <div class="timeline-item">
                <div class="marker"></div>
                <h4>6. Decisión Final</h4>
                <p>Escalar, Iterar, o Cerrar y aprender.</p>
            </div>
        </div>
    </div>
</section>

<style>
    .grid-process {
        display: grid;
        grid-template-columns: 1fr;
        gap: 3rem;
    }
    @media (min-width: 1024px) {
        .grid-process {
            grid-template-columns: 1fr 2fr;
            gap: 5rem;
        }
        .sticky-col {
            position: sticky;
            top: 6rem;
            height: fit-content;
        }
    }
    .desc {
        color: var(--text-gray);
        margin-bottom: 2rem;
    }

    .principles-card {
        background: var(--bg-card);
        border: 1px solid var(--border-color);
        padding: 1.5rem;
        border-radius: var(--radius-lg);
        margin-top: 2rem;
    }
    .principles-card ul li {
        margin-top: 0.75rem;
        color: var(--text-gray);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    .dot {
        width: 6px;
        height: 6px;
        background: var(--color-primary);
        border-radius: 50%;
    }

    .timeline {
        border-left: 1px solid var(--border-color);
        padding-left: 2rem;
        display: flex;
        flex-direction: column;
        gap: 3rem;
    }
    .timeline-item {
        position: relative;
        cursor: default;
    }

    .marker {
        position: absolute;
        left: -41px;
        top: 0;
        width: 20px;
        height: 20px;
        background: #3f3f46;
        border: 4px solid var(--bg-dark);
        border-radius: 50%;
        transition: all 0.3s;
    }

    .timeline-item:hover .marker {
        background: var(--color-primary);
        box-shadow: 0 0 10px rgba(16, 185, 129, 0.5);
    }
    .timeline-item:hover h4 {
        color: var(--color-primary);
        transition: color 0.3s;
    }

    .timeline-item h4 {
        font-size: 1.25rem;
        margin-bottom: 0.5rem;
        transition: color 0.3s;
    }
    .timeline-item p {
        color: var(--text-muted);
        font-size: 0.95rem;
    }
</style>
