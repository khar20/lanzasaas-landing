<section id="problema" class="section bg-black">
    <div class="bg-radial"></div>
    <div class="container">
        <div class="text-center mb-large">
            <h2>EL PROBLEMA ACTUAL</h2>
            <p class="desc-lg">
                Empatía real con el mercado. <span class="text-red"
                    >Hoy el sistema está roto.</span>
            </p>
        </div>

        <div class="grid-3-col">
            <article class="card card-hover-red">
                <div class="card-icon-bg red">
                    <span class="material-symbols-outlined">person_off</span>
                </div>
                <h3>Founders Solitarios</h3>
                <p>
                    Founders con grandes ideas, pero solos. Sin equipo técnico
                    para ejecutar ni socios para contrastar visión.
                </p>
            </article>
            <article class="card card-hover-red">
                <div class="card-icon-bg red">
                    <span class="material-symbols-outlined">work_off</span>
                </div>
                <h3>Talento sin Propósito</h3>
                <p>
                    Técnicos y marketers atrapados en trabajos sin propósito.
                    Mucho talento infrautilizado picando código sin ver impacto.
                </p>
            </article>
            <article class="card card-hover-red">
                <div class="card-icon-bg red">
                    <span class="material-symbols-outlined">school</span>
                </div>
                <h3>Formación sin Ejecución</h3>
                <p>
                    Personas formándose sin aplicar nada real. Mucho contenido,
                    poca ejecución. Títulos vacíos sin proyectos reales.
                </p>
            </article>
        </div>

        <div class="alert-box">
            <p>
                La mayoría: <span class="text-red"
                    >O emprende solo y se quema, O trabaja para otros sin
                    construir nada propio.</span>
            </p>
        </div>
    </div>
</section>

<style>
    .bg-black {
        background-color: black;
    }
    .bg-radial {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: radial-gradient(
            circle at center,
            rgba(24, 24, 27, 0.5),
            black
        );
        pointer-events: none;
    }
    .mb-large {
        margin-bottom: 4rem;
    }
    .desc-lg {
        font-size: 1.25rem;
        color: var(--text-muted);
        margin-top: 1rem;
    }

    .card {
        background: var(--bg-card);
        border: 1px solid var(--border-color);
        padding: 2rem;
        border-radius: var(--radius-xl);
        transition: 0.3s;
        height: 100%;
        position: relative;
        z-index: 1;
    }
    .card h3 {
        font-size: 1.25rem;
        font-weight: 700;
        margin-bottom: 1rem;
    }
    .card p {
        font-size: 0.9rem;
        color: var(--text-muted);
    }

    .card-icon-bg {
        width: 3.5rem;
        height: 3.5rem;
        border-radius: var(--radius-md);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
    }
    .card-icon-bg.red {
        background: rgba(239, 68, 68, 0.1);
        color: #ef4444;
    }
    .card-hover-red:hover {
        border-color: rgba(239, 68, 68, 0.5);
    }

    .alert-box {
        margin-top: 4rem;
        background: rgba(69, 10, 10, 0.3);
        border: 1px solid rgba(127, 29, 29, 0.5);
        padding: 1.5rem;
        border-radius: var(--radius-lg);
        display: inline-block;
        text-align: center;
        width: 100%;
        position: relative;
        z-index: 1;
    }
</style>
