---
const problems = [
    {
        title: "Visión Fragmentada",
        desc: "Desarrollar código es solo el comienzo. Un negocio exitoso necesita estrategia de mercado integrada desde el primer día.",
        icon: "🧩",
    },
    {
        title: "Desarrollo Sin Fin",
        desc: "Lanzar es aprender. Construimos solo lo esencial para validar tracción con usuarios reales.",
        icon: "⏳",
    },
    {
        title: "Construir en el Vacío",
        desc: "La estrategia de mercado no es opcional. Definimos cómo llegarás a tus clientes antes de escribir la primera línea de código.",
        icon: "🌫️",
    },
];
---

<section class="problem">
    <div class="container">
        <div class="header">
            <h2>Resolvemos los obstáculos reales de crear software</h2>
            <p>
                El problema no es tu idea. <strong
                    >Es no tener un sistema completo para ejecutarla.</strong>
            </p>
        </div>
        <div class="grid">
            {
                problems.map((item) => (
                    <div class="card">
                        <div class="icon">{item.icon}</div>
                        <h3>{item.title}</h3>
                        <p>{item.desc}</p>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    .problem {
        padding: 100px 0;
        background: linear-gradient(to bottom, var(--bg-dark), #09120f);
    }

    .header {
        text-align: center;
        margin-bottom: 70px;
    }

    .header h2 {
        font-size: 2.2rem;
        margin-bottom: 15px;
    }

    .header p {
        font-size: 1.1rem;
        color: var(--text-gray);
    }
    .header strong {
        color: var(--text-white);
    }

    .grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 30px;
    }

    .card {
        background: var(--bg-card);
        padding: 35px;
        border-radius: 16px;
        border: 1px solid rgba(255, 255, 255, 0.05);
        transition: transform 0.2s;
        height: 100%;
    }

    .card:hover {
        transform: translateY(-5px);
        background: var(--bg-card-hover);
        border-color: rgba(74, 222, 128, 0.1);
    }

    .icon {
        font-size: 2rem;
        margin-bottom: 1.5rem;
    }

    h3 {
        font-size: 1.25rem;
        margin-bottom: 1rem;
    }

    p {
        font-size: 0.95rem;
        color: var(--text-gray);
        line-height: 1.6;
    }

    @media (max-width: 768px) {
        .grid {
            grid-template-columns: 1fr;
        }
    }
</style>
