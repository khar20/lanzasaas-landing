<section id="que-es" class="section">
    <div class="container grid-2-col">
        <div class="content-text">
            <div class="section-header">
                <h2>QUÉ ES LANZASAAS</h2>
                <p class="subtitle">Explicación simple, sin humo</p>
            </div>
            <p class="desc">
                LanzaSaaS es la primera vertical de <strong
                    >Lanza Ecosystem</strong
                >: un entorno donde personas con visión y talento se unen para
                crear SaaS reales desde cero.
            </p>
            <ul class="check-list">
                <li class="negative">
                    <span class="material-symbols-outlined icon-red">close</span
                    > No se simula emprendimiento
                </li>
                <li class="negative">
                    <span class="material-symbols-outlined icon-red">close</span
                    > No se aprende solo teoría
                </li>
                <li class="negative">
                    <span class="material-symbols-outlined icon-red">close</span
                    > No se trabaja por horas sin sentido
                </li>
                <li class="positive-box">
                    <span class="material-symbols-outlined">check_circle</span> Se
                    construyen empresas reales, en equipo
                </li>
            </ul>
        </div>

        <div class="ecosystem-visual">
            <!-- Líneas de conexión SVG -->
            <svg class="connection-lines" viewBox="0 0 400 350">
                <!-- Centro a Arriba (Team) -->
                <line
                    x1="200"
                    y1="175"
                    x2="200"
                    y2="60"
                    stroke="rgba(16, 185, 129, 0.2)"
                    stroke-width="2"></line>
                <!-- Centro a Izquierda (Growth) -->
                <line
                    x1="200"
                    y1="175"
                    x2="80"
                    y2="290"
                    stroke="rgba(16, 185, 129, 0.2)"
                    stroke-width="2"></line>
                <!-- Centro a Derecha (Equity) -->
                <line
                    x1="200"
                    y1="175"
                    x2="320"
                    y2="290"
                    stroke="rgba(16, 185, 129, 0.2)"
                    stroke-width="2"></line>
            </svg>

            <div class="cards-layout">
                <!-- CENTRO: SaaS Product -->
                <div class="mini-card saas-product center-card">
                    <span class="material-symbols-outlined icon-green"
                        >rocket</span
                    >
                    <span>SaaS Product</span>
                </div>

                <!-- TRIÁNGULO: Team (Arriba) -->
                <div class="mini-card float-delay-1 top-card">
                    <span class="material-symbols-outlined">groups</span>
                    <span>Team</span>
                </div>

                <!-- TRIÁNGULO: Growth (Abajo Izq) -->
                <div class="mini-card float-delay-2 bottom-left-card">
                    <span class="material-symbols-outlined">trending_up</span>
                    <span>Growth</span>
                </div>

                <!-- TRIÁNGULO: Equity (Abajo Der) -->
                <div class="mini-card float-delay-3 bottom-right-card">
                    <span class="material-symbols-outlined icon-green"
                        >pie_chart</span
                    >
                    <span>Equity</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .grid-2-col {
        display: grid;
        grid-template-columns: 1fr;
        gap: 4rem;
    }
    @media (min-width: 768px) {
        .grid-2-col {
            grid-template-columns: 1fr 1fr;
        }
    }

    .section-header {
        margin-bottom: 2rem;
        padding-left: 1.5rem;
        border-left: 4px solid var(--color-primary);
    }
    .subtitle {
        color: var(--color-primary);
        font-size: 1.2rem;
        font-weight: 500;
        margin-top: 0.5rem;
    }
    .desc {
        color: var(--text-gray);
        font-size: 1.1rem;
        margin-bottom: 2rem;
    }

    .check-list li {
        display: flex;
        align-items: center;
        gap: 1rem;
        font-size: 0.95rem;
        margin-top: 1rem;
    }
    .negative {
        color: var(--text-muted);
    }
    .icon-red {
        color: #ef4444;
    }
    .positive-box {
        background: var(--color-primary-dim);
        border: 1px solid rgba(16, 185, 129, 0.3);
        padding: 1rem;
        border-radius: var(--radius-md);
        color: white;
        font-weight: 700;
    }

    .ecosystem-visual {
        position: relative;
        min-height: 400px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .connection-lines {
        position: absolute;
        width: 100%;
        height: 100%;
        max-width: 400px;
        max-height: 350px;
        z-index: 0;
        pointer-events: none;
    }

    .cards-layout {
        position: relative;
        width: 100%;
        max-width: 400px;
        height: 350px;
        z-index: 2;
    }

    .mini-card {
        background: var(--bg-card);
        border: 1px solid rgba(16, 185, 129, 0.3);
        padding: 1.2rem;
        border-radius: var(--radius-lg);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
        width: 110px;
        height: 100px;
        position: absolute;
        animation: float 6s ease-in-out infinite;
        text-align: center;
    }

    .mini-card span:last-child {
        font-size: 0.85rem;
        font-weight: 700;
        line-height: 1.1;
    }

    .center-card {
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 10;
        width: 130px;
        height: 120px;
        animation: scale-pulse 4s ease-in-out infinite;
    }

    .top-card {
        top: 0;
        left: 50%;
        transform: translateX(-50%);
    }

    .bottom-left-card {
        bottom: 0;
        left: 0;
    }

    .bottom-right-card {
        bottom: 0;
        right: 0;
    }

    .saas-product {
        border-color: var(--color-primary);
        background:
            linear-gradient(rgba(16, 185, 129, 0.05), rgba(16, 185, 129, 0.05)),
            var(--bg-card);
    }

    @keyframes float {
        0% {
            margin-top: 0px;
        }
        50% {
            margin-top: -10px;
        }
        100% {
            margin-top: 0px;
        }
    }

    @keyframes scale-pulse {
        0% {
            transform: translate(-50%, -50%) scale(1);
            box-shadow: 0 0 20px rgba(16, 185, 129, 0.2);
        }
        50% {
            transform: translate(-50%, -50%) scale(1.05);
            box-shadow: 0 0 30px rgba(16, 185, 129, 0.4);
        }
        100% {
            transform: translate(-50%, -50%) scale(1);
            box-shadow: 0 0 20px rgba(16, 185, 129, 0.2);
        }
    }

    .float-delay-1 {
        animation-delay: 1s;
    }
    .float-delay-2 {
        animation-delay: 2s;
    }
    .float-delay-3 {
        animation-delay: 1.5s;
    }

    .icon-green {
        color: var(--color-primary);
    }
</style>
