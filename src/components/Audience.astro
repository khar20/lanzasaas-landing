---

---

<section class="core-focus">
    <div class="container grid">
        <div class="content">
            <h2>
                Un ecosistema diseñado para <span class="text-green"
                    >resultados.</span>
            </h2>
            <div class="body-text">
                <p>
                    El LAB amplifica lo que funciona y elimina lo que distrae.
                </p>
                <p>
                    Nos enfocamos en <strong>ejecución real</strong> y en crear activos
                    que generan valor tangible.
                </p>
            </div>
        </div>
        <div class="image-wrapper">
            <img
                src="https://images.unsplash.com/photo-1522071820081-009f0129c71c?q=80&w=2070&auto=format&fit=crop"
                alt="Team Working"
            />
            <div class="overlay"></div>
        </div>
    </div>
</section>

<style>
    .core-focus {
        padding: 100px 0;
        background: #080f0c;
    }

    .grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
    }

    h2 {
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
        line-height: 1.1;
    }

    .text-green {
        color: var(--primary-green);
    }

    .body-text p {
        font-size: 1.125rem;
        color: var(--text-gray);
        margin-bottom: 1rem;
        line-height: 1.6;
    }

    .body-text strong {
        color: var(--text-white);
        font-weight: 600;
    }

    .image-wrapper {
        position: relative;
        border-radius: 24px;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.1);
        aspect-ratio: 16/9;
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
    }

    .image-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .image-wrapper .overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(to top right, var(--bg-dark), transparent);
        opacity: 0.6;
    }

    @media (max-width: 768px) {
        .grid {
            grid-template-columns: 1fr;
            gap: 40px;
        }
    }
</style>
