---

---

<nav>
    <div class="container nav-content">
        <div class="logo">
            <span class="icon">🚀</span>
            <span>LanzaSaaS LAB</span>
        </div>
        <a href="#apply" class="btn btn-primary">Solicitar acceso</a>
    </div>
</nav>

<style>
    nav {
        position: fixed;
        top: 0;
        width: 100%;
        padding: 20px 0;
        z-index: 100;
        background: rgba(5, 10, 8, 0.9);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .nav-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo {
        display: flex;
        align-items: center;
        gap: 10px;
        font-weight: 800;
        font-size: 1.1rem;
        letter-spacing: -0.5px;
    }

    .icon {
        font-size: 1.2rem;
    }
</style>
