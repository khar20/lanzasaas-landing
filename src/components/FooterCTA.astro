---

---

<section class="footer-cta">
    <div class="container">
        <div class="lock-icon">🔒</div>
        <h2>Acceso limitado por diseño</h2>
        <p>
            Mantenemos el grupo pequeño para asegurar la calidad, el compromiso
            y que cada proyecto reciba la atención que merece.
        </p>

        <div class="action">
            <a href="#" class="btn btn-primary btn-wide"
                >Aplicar a LanzaSaaS LAB</a
            >
            <span class="note"
                >Si encajas, recibirás una invitación privada en menos de 48h.</span
            >
        </div>
    </div>
</section>

<style>
    .footer-cta {
        padding: 100px 0 150px;
        text-align: center;
        background: radial-gradient(
            circle at center,
            #0c1812 0%,
            var(--bg-dark) 70%
        );
    }

    .lock-icon {
        font-size: 2.5rem;
        margin-bottom: 20px;
        color: var(--primary-green);
    }

    h2 {
        font-size: 2.5rem;
        margin-bottom: 20px;
    }

    p {
        color: var(--text-gray);
        max-width: 600px;
        margin: 0 auto 40px;
    }

    .action {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 15px;
    }

    .btn-wide {
        padding: 16px 40px;
        font-size: 1.1rem;
    }

    .note {
        font-size: 0.8rem;
        color: #52525b;
    }
</style>
