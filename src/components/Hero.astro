---

---

<section class="hero">
    <div class="container hero-content">
        <div class="pill-container">
            <p class="core-value">Combinamos talento con un proceso probado</p>
        </div>

        <h1>
            Transforma tus ideas en <br />
            <span class="highlight">negocios de software reales.</span>
        </h1>

        <p class="subtitle">
            LanzaSaaS LAB es el lugar donde builders, marketers y estrategas
            construyen y escalan productos digitales que generan ingresos.
        </p>

        <div class="cta-wrapper">
            <a href="#apply" class="btn btn-primary btn-large">Únete al LAB</a>
        </div>
    </div>

    <div class="bg-dots"></div>
</section>

<style>
    .hero {
        position: relative;
        padding: 160px 0 100px;
        text-align: center;
        overflow: hidden;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .pill-container {
        margin-bottom: 2rem;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 8px 20px;
        border-radius: 50px;
    }

    .core-value {
        font-size: 0.85rem;
        color: var(--primary-green);
        font-weight: 500;
    }

    .text-white {
        color: var(--text-white);
    }

    h1 {
        font-size: 3.5rem;
        font-weight: 800;
        margin-bottom: 1.5rem;
        letter-spacing: -0.02em;
        line-height: 1.1;
    }

    .highlight {
        background: -webkit-linear-gradient(
            45deg,
            #ffffff,
            var(--primary-green)
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .subtitle {
        font-size: 1.25rem;
        color: var(--text-gray);
        max-width: 700px;
        margin-bottom: 2.5rem;
        line-height: 1.6;
    }

    .btn-large {
        padding: 16px 40px;
        font-size: 1.1rem;
    }

    .bg-dots {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: radial-gradient(
            rgba(255, 255, 255, 0.1) 1px,
            transparent 1px
        );
        background-size: 40px 40px;
        opacity: 0.3;
        z-index: 1;
        mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }
    }
</style>
