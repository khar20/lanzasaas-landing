<section class="hero">
    <div class="hero-bg-grid"></div>
    <div class="hero-glow"></div>

    <div class="container hero-content">
        <div class="badge">
            <span class="pulse-dot"></span>
            Abriendo batch para nuevos founders y talentos
        </div>

        <h1>
            Construye empresas SaaS <br /><span class="text-gradient"
                >en equipo.</span>
        </h1>
        <h2>Con visión, equity y propósito.</h2>

        <p class="hero-desc">
            LanzaSaaS es un ecosistema de creación de empresas SaaS donde las
            ideas se convierten en productos reales, el talento crece trabajando
            en proyectos reales y el valor se reparte con equity desde el
            inicio.
        </p>

        <div class="hero-note">
            <p>
                <span class="highlight">No somos un curso.</span> No somos una comunidad
                más. Somos un sistema para construir empresas con personas alineadas.
            </p>
        </div>

        <div class="roles-selector">
            <p class="roles-label">ELIGE TU ROL Y ENTRA AL ECOSISTEMA</p>
            <div class="roles-buttons">
                <!-- Enlaces añadidos -->
                <a href="/founder" class="btn btn-outline role-btn">
                    <span class="material-symbols-outlined">psychology</span> Soy
                    Founder
                </a>
                <a href="/lab#tech" class="btn btn-outline role-btn">
                    <span class="material-symbols-outlined">code</span> Soy Técnico
                </a>
                <a href="/lab#marketing" class="btn btn-outline role-btn">
                    <span class="material-symbols-outlined">campaign</span> Soy Marketer
                </a>
                <a href="/talent" class="btn btn-outline role-btn">
                    <span class="material-symbols-outlined">school</span> Desde Cero
                </a>
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        position: relative;
        padding: 6rem 1.5rem;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 85vh;
        overflow: hidden;
    }
    .hero-bg-grid {
        position: absolute;
        inset: 0;
        background-image:
            linear-gradient(to right, #27272a 1px, transparent 1px),
            linear-gradient(to bottom, #27272a 1px, transparent 1px);
        background-size: 40px 40px;
        opacity: 0.05;
        pointer-events: none;
    }
    .hero-glow {
        position: absolute;
        top: -100px;
        left: 50%;
        transform: translateX(-50%);
        width: 600px;
        height: 600px;
        background: var(--color-primary);
        filter: blur(150px);
        opacity: 0.15;
        border-radius: 50%;
        pointer-events: none;
    }
    .hero-content {
        position: relative;
        z-index: 10;
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2rem;
    }

    .badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.4rem 1rem;
        border-radius: 99px;
        background: var(--color-primary-dim);
        border: 1px solid rgba(16, 185, 129, 0.3);
        color: var(--color-primary);
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
    }
    .pulse-dot {
        width: 8px;
        height: 8px;
        background: var(--color-primary);
        border-radius: 50%;
        animation: pulse 2s infinite;
    }
    @keyframes pulse {
        0% {
            box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.7);
        }
        70% {
            box-shadow: 0 0 0 10px rgba(16, 185, 129, 0);
        }
        100% {
            box-shadow: 0 0 0 0 rgba(16, 185, 129, 0);
        }
    }

    h1 {
        font-size: clamp(2.5rem, 5vw, 4.5rem);
        font-weight: 800;
        letter-spacing: -0.03em;
        line-height: 1.1;
    }
    .text-gradient {
        background: linear-gradient(to right, var(--color-primary), #86efac);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    h2 {
        font-size: 1.5rem;
        color: var(--text-gray);
        font-weight: 400;
    }
    .hero-desc {
        max-width: 700px;
        color: var(--text-gray);
        font-size: 1.1rem;
    }

    .hero-note {
        background: rgba(18, 18, 18, 0.8);
        border: 1px solid var(--border-color);
        padding: 1rem 1.5rem;
        border-radius: var(--radius-lg);
        max-width: 600px;
        font-size: 0.9rem;
        color: var(--text-gray);
    }
    .highlight {
        color: var(--color-primary);
        font-weight: 700;
    }

    .roles-selector {
        width: 100%;
        margin-top: 2rem;
    }
    .roles-label {
        font-size: 0.8rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: #6b7280;
        margin-bottom: 1rem;
    }
    .roles-buttons {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
    }

    .role-btn {
        transition: all 0.3s ease;
        text-decoration: none;
    }

    .role-btn:hover {
        background-color: var(--color-primary);
        color: black;
        border-color: var(--color-primary);
        box-shadow: 0 0 20px rgba(16, 185, 129, 0.4);
        transform: translateY(-2px);
    }
</style>
