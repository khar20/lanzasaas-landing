---

---

<section class="hero">
    <div class="container hero-content">
        <div class="pill">● Comunidad Privada</div>
        <h1>
            Donde las ideas se <br /> convierten en <span class="highlight"
                >SaaS reales.</span>
        </h1>
        <p class="subtitle">
            LanzaSaaS LAB es una comunidad privada de builders, marketers y
            estrategas que se asocian para crear, lanzar y escalar software
            rentable.
        </p>
        <p class="disclaimer">
            No es formación. No es networking. <span class="text-green"
                >Es ejecución y co-funding.</span
            >
        </p>
        <div class="cta-wrapper">
            <a href="#" class="btn btn-primary btn-large"
                >Solicitar acceso ahora</a
            >
            <span class="meta">Aplicación requerida · Plazas limitadas</span>
        </div>
    </div>

    <!-- decorative background dots -->
    <div class="bg-dots"></div>
</section>

<style>
    .hero {
        position: relative;
        padding: 160px 0 100px;
        text-align: center;
        overflow: hidden;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .pill {
        background: rgba(74, 222, 128, 0.1);
        color: var(--primary-green);
        border: 1px solid rgba(74, 222, 128, 0.2);
        padding: 6px 16px;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
        letter-spacing: 1px;
        text-transform: uppercase;
        margin-bottom: 2rem;
    }

    h1 {
        font-size: 3.5rem;
        font-weight: 800;
        margin-bottom: 1.5rem;
        letter-spacing: -0.02em;
    }

    .highlight {
        color: var(--primary-green);
    }

    .subtitle {
        font-size: 1.125rem;
        color: var(--text-gray);
        max-width: 600px;
        margin-bottom: 1rem;
    }

    .disclaimer {
        font-size: 0.9rem;
        font-weight: 600;
        margin-bottom: 3rem;
    }

    .cta-wrapper {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1rem;
    }

    .btn-large {
        padding: 16px 32px;
        font-size: 1.1rem;
    }

    .meta {
        font-size: 0.7rem;
        color: var(--text-gray);
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .bg-dots {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: radial-gradient(
            rgba(255, 255, 255, 0.1) 1px,
            transparent 1px
        );
        background-size: 40px 40px;
        opacity: 0.3;
        z-index: 1;
        mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }
    }
</style>
