---

---

<section class="hero">
    <div class="container hero-content">
        <div class="pill">🚀 De idea a MVP validado. Sin humo.</div>

        <h1>
            Convierte una idea en un <br />
            <span class="highlight">SaaS real, validado y con equipo</span>
        </h1>

        <div class="negatives">
            <span>✕ No es formación.</span>
            <span>✕ No es networking.</span>
            <span>✕ No es una agencia.</span>
        </div>

        <p class="subtitle">
            LanzaSaaS LAB es un ecosistema de ejecución donde ideas con
            potencial se convierten en SaaS reales, pasando de la teoría al
            mercado con sistema, equipo y <strong>skin in the game</strong>.
        </p>

        <div class="cta-wrapper">
            <a href="#apply" class="btn btn-primary btn-large"
                >Solicitar acceso al LAB</a
            >
        </div>
    </div>

    <div class="bg-dots"></div>
</section>

<style>
    .hero {
        position: relative;
        padding: 160px 0 100px;
        text-align: center;
        overflow: hidden;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .pill {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 8px 16px;
        border-radius: 50px;
        font-size: 0.85rem;
        color: var(--text-white);
        margin-bottom: 2rem;
        font-weight: 600;
    }

    h1 {
        font-size: 3.5rem;
        font-weight: 800;
        margin-bottom: 1.5rem;
        letter-spacing: -0.02em;
        line-height: 1.1;
    }

    .highlight {
        color: var(--primary-green);
    }

    .negatives {
        display: flex;
        gap: 20px;
        margin-bottom: 2rem;
        font-weight: 600;
        color: var(--accent-red);
        font-size: 0.95rem;
        flex-wrap: wrap;
        justify-content: center;
    }

    .subtitle {
        font-size: 1.2rem;
        color: var(--text-gray);
        max-width: 750px;
        margin-bottom: 2.5rem;
        line-height: 1.6;
    }

    .subtitle strong {
        color: var(--text-white);
        text-decoration: underline;
        text-decoration-color: var(--primary-green);
    }

    .btn-large {
        padding: 18px 40px;
        font-size: 1.1rem;
    }

    .bg-dots {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: radial-gradient(
            rgba(74, 222, 128, 0.1) 1px,
            transparent 1px
        );
        background-size: 40px 40px;
        opacity: 0.3;
        z-index: 1;
        mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }
        .negatives {
            flex-direction: column;
            gap: 10px;
        }
    }
</style>
