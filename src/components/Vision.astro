<section id="vision" class="section text-center bg-black">
    <div class="container max-w-800">
        <span class="badge-outline">Aspiración y Propósito</span>
        <h2 class="mt-4">VISIÓN A LARGO PLAZO</h2>
        <p class="desc-lg mt-4">
            LanzaSaaS es solo el inicio. Es un sistema para crear empresas,
            conectar talento y transformar trabajo en patrimonio.
        </p>
        <div class="quote-box">
            <span class="material-symbols-outlined quote-icon"
                >format_quote</span
            >
            <blockquote>
                "Imagina un sistema donde a la gente le gusta su trabajo. Donde
                sienten que es suyo. Donde crecer significa crear valor real."
            </blockquote>
        </div>
    </div>
</section>

<style>
    .bg-black {
        background-color: black;
    }
    .max-w-800 {
        max-width: 800px;
        margin: 0 auto;
    }
    .mt-4 {
        margin-top: 1.5rem;
    }
    .badge-outline {
        border: 1px solid rgba(16, 185, 129, 0.3);
        color: var(--color-primary);
        padding: 0.4rem 1rem;
        border-radius: 99px;
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
    }
    .desc-lg {
        font-size: 1.2rem;
        color: var(--text-gray);
    }

    .quote-box {
        position: relative;
        margin-top: 4rem;
        padding: 2rem;
    }
    .quote-icon {
        position: absolute;
        top: -2rem;
        left: -1rem;
        font-size: 8rem;
        color: rgba(16, 185, 129, 0.1);
        transform: rotate(10deg);
        z-index: 0;
    }
    blockquote {
        font-size: 1.5rem;
        font-style: italic;
        color: white;
        position: relative;
        z-index: 1;
        line-height: 1.4;
    }
    @media (min-width: 768px) {
        blockquote {
            font-size: 2rem;
        }
    }
</style>
